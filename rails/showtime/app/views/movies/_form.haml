
= form_with(model: movie) do |form|
  = form.label :title
  = form.text_field :title
  = form.label :release_year
  = form.select :release_year, (1900..2050).to_a
  = form.collection_check_boxes :genre_ids, genres, :id, :name
  -# - genres.each do |genre|
  -#   = label_tag 'movie[genre_ids][]', genre.name
  -#   = check_box_tag 'movie[genre_ids][]', genre.id
  = form.submit 'Save'

