<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl"><%= t('contract.title.new') %></h1>

  <% content_for :head do %>
    <title><%= t('contract.title.new') %></title>
  <% end %>

  <%= render "form", contract: @contract %>

  <%= link_to t('contract.button.back'), contracts_path, class: "ml-2 rounded-lg py-3 px-5 bg-gray-300 inline-block font-medium" %>
</div>
