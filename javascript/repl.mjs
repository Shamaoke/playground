// Since ES modules is still an experimental feature in node ~>10.0,
// to use them and --experimental-modules option to the `node'
// executable.

import Repl from 'repl'

Repl.start('~> ')

// vim: set textwidth=70 colorcolumn=70:
