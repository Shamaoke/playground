struct { int x, y, z; } point = { .y = 10, .z = 20, .x = 30 };

struct {
  int x, y, z;
} point = {
  .x = 30,
  .y = 10,
  .z = 20
};

struct {
  int x, y, z;
}
point = {
  .x = 30,
  .y = 10,
  .z = 20
};

struct
{ int x,
  int y,
  int z }

point =
{ .x = 30,
  .y = 10,
  .z = 20 };

typedef struct
{ int x,
  int y,
  int z } point;

point =
{ .x = 30,
  .y = 10,
  .z = 20 };
