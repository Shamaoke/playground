
An example application showing calls of the constructor and the destructor of a
class when an object of the class is created using the `std::make_unique` method
and destroyed when the variable owing the object goes out of its scope.

