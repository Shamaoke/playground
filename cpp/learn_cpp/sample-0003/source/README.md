
An example application showing calls of the constructor and the destructor of a
class when an object of the class is created in a frame on the stack and destroyed
when the frame is popped off the stack.

